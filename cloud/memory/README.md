# Memory Service

Stores and retrieves cross-session context for each user's digital employee.

## Planned features
- Per-user memory namespacing
- Semantic search over past interactions
- Data export / deletion (user data ownership)
- Optional local-only mode
